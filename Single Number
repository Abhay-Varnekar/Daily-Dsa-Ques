class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int onlyNum = 0;
    for (size_t i = 0; i < nums.size(); ++i) {
        onlyNum ^= nums[i];
    }
    return onlyNum;
}     
};
